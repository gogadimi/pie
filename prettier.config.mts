import type { Config } from 'prettier';

const config: Config = {
  arrowParens: 'always',
  singleQuote: true,
  tabWidth: 2,
  trailingComma: 'all',
  semi: true,
  printWidth: 100,
};

export default config;
